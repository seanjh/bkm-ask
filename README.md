# bkm

## Setup
First, install the dependencies. 

This notebook and its supporting scripts are developed in Python 3, so the ```pip3``` command might be required in place of ```pip``` below, depending on the system settings.

    pip install -r requirements.txt
    ipython notebook
